node_modules/
data/
*.db
*.db-journal
.env
.env.local
bun.lock
bun.lockb
package-lock.json
tsconfig.tsbuildinfo
dist/
src/frontend/dist/
admiral
# every compiled variant: admiral.exe, .new, .prev, .backup, and any future suffix.
# ~100MB each — one `git add -A` without this would put one in history permanently.
admiral*.exe

# ad-hoc game-data dumps in the repo root
/morg_*.json
/edw_*.json
/devastator.json
/fleet_inventory.json
/full_inventory.json
/profiles.json
/profiles2.json
/recipe_*.json
/railgun_recipes.json
/piercing_railgun_ii.json
*.bun-build
.*.bun-build
# Claude Code machine-local settings (permissions etc.) — not shared
.claude/settings.local.json

# runtime output logs
admiral-stdout.log
admiral-stderr.log

# Admiral watcher state (regenerated; not worth versioning)
data/needs-admiral.jsonl
data/needs-admiral.ack

# location-specific marker that lives untracked in the OneDrive backup clone only
_MOVED__DO_NOT_RUN.txt
